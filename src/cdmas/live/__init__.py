"""Live single-process MAS demo: runs the real fleet and streams its events to the
dashboard over WebSocket. Additive — the validator harness, simulator API, and the
prebaked replay are untouched."""
